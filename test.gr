module Test

from "./toString.gr" include ToString
use ToString.{ toString }
from "list" include List
from "string" include String
let lst = List.init(500_000, i => 1)
let str = toString(lst)
let mut count = 0
String.forEachChar(c => if (c == '1') count += 1, str)
print(toString(str))
print(count)
